{
  "name": "torn-commander-sandbox",
  "version": "15.0.0",
  "private": true,
  "description": "Persistent mobile-friendly MTG Commander sandbox with card intelligence, stack, priority, assisted combat, triggers, mana, attachments, special layouts and undo history.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/sw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.22.0",
    "socket.io": "^4.8.1"
  }
}
