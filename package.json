{
  "name": "torn-commander-sandbox",
  "version": "5.0.0",
  "private": true,
  "description": "A complete mobile-friendly 2-6 player MTG Commander sandbox for the Torn community.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/sw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
