{
  "name": "torn-commander-sandbox",
  "version": "20.0.0",
  "private": true,
  "description": "Persistent Arena-style 2-6 player MTG Commander table with card intelligence, stack, priority, assisted combat, spectators, replays and accessibility settings.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/sw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.22.0",
    "socket.io": "^4.8.1"
  }
}
