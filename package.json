{
  "name": "torn-commander-sandbox",
  "version": "37.0.0",
  "private": true,
  "description": "Arena-style MTG table with locked Official Commander and Brawl presets, Custom Rule Zero rooms, 2–6 seats, AI Test Lab and persistent games.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check ai-engine.js && node --check public/app.js && node --check public/sw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.22.0",
    "socket.io": "^4.8.1"
  }
}
