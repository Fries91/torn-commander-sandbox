{
  "name": "torn-commander-sandbox",
  "version": "35.0.0",
  "private": true,
  "description": "Arena-style 2\u20136 player Commander table with Solo AI Test Lab, optional bot seats, deck strategy profiles, tactical bot decisions and Judge Mode.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check ai-engine.js && node --check public/app.js && node --check public/sw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.22.0",
    "socket.io": "^4.8.1"
  }
}
